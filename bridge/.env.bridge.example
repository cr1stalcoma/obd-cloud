# Bridge on Windows PC — copy to .env and load before run
# set / load in PowerShell: Get-Content .env.bridge | ForEach-Object { ... } or use dotenv

PUBLIC_API_URL=https://obd.lexora.by
SCANNER_ID=565300
SCANNER_SECRET=change-me-openssl-rand-hex-16
COM_PORT=COM3
